# C#_LAB_1
# Епанова Алина, КМБ-2, Лабораторная №7
## Задание 1. Текстовые файлы.
### Текст задачи
Найти разность максимального и минимального элементов.
### Алгорит решения
1. Реализован статический метод заполения файла, который создает объект SteamWriter для записи в файл, с помощью цикла генерирует случайное число в диапазоне от 0 до 100 на каждой итерации и записывает его в новую строку. После завершения цила файл закрывается.
2. Метод DifferentMaxMin, который находит разность между максимальным и минимальным значением файла. Открываем файла для чтения с помощью StreamReader, до тех пор пока файл не закончился считываем строку, преобразовываем ее в число и последовательно обновляем значения минимума и максимума. Возвращаем разность.
3. Метод вывода содержимого файла PrintFile, построчно читает файл и выводит его содержимое.
### Тестирование
<img width="704" height="342" alt="image" src="https://github.com/user-attachments/assets/8fec7b68-e6a3-4a22-b68e-c4a9fc7468c3" />

## Задание 2. Текстовые файлы.
### Текст задачи
Вычислить минимальный элемент.
### Алгорит решения
1. Реализован метод заполения файла с несколькими числами в строку через пробел.
2. Метод поиска минимума: читаем файл построчно, каждую строку разбиваем на числа (с помощью Split), преобразуем строки в число, сравниваем и обновляем минимум. Возвращаем найденный минимум.
### Тестирование
<img width="599" height="280" alt="image" src="https://github.com/user-attachments/assets/96a5e0eb-c289-4219-846e-38ef2ecc49e3" />

## Задание 3. Текстовые файлы.
### Текст задачи
Переписать в другой файл строки, начинающиеся с заданного символа.
### Алгорит решения
1. Реализован метод CopyLine, которые копирует строки, начинающиеся с заданного символа. Открываем исходный файл для чтения и новый для записи. Читаем файл построчно и для каждой строки проверяем, чтобы она не была пустой и на совпадение с заданным символом. Если совпадает - записываем в новый файл.
### Тестирование
<img width="729" height="381" alt="image" src="https://github.com/user-attachments/assets/607c907a-52fd-4861-8e35-1c834c321376" />

## Задание 4. Бинарные файлы.
### Текст задачи
Получить в новом файле все компоненты исходного файла, которые делятся на m и не делятся
на n.
### Алгорит решения
1. Реализован статический метод заполнения бинарного файла: открываеи бинарный файл для записи, генерируем случайные числа и каждое число записывается в файл в бинарном виде.
2. Метод фильтрации данных: открываем исходный файл для чтения и новый для записи, пока не конец файла - считываем число и проверяем условие (num % m == 0 && num % n != 0), если оно выполняется записываем в новый файл.
### Тестирование
<img width="663" height="129" alt="image" src="https://github.com/user-attachments/assets/13b7c9ed-f784-44ce-b647-7cdfb6cd67a7" />

## Задание 5. Бинарные файлы.
### Текст задачи
Информация о багаже пассажира описывается массивом, где каждый элемент содержит название
единицы багажа (чемодан, сумка, коробка и т.д.) и ее массу. Дан файл, содержащий сведения о
багаже нескольких пассажиров. Выяснить, имеется ли пассажир, багаж которого состоит из
одной единицы багажа массой менее m кг.
### Алгорит решения
1. Реализован статический метод заполнения файла XML, которые формирует структуру данных в виде списка списков, где внутренний список представляет собой багаж пассажира.
2. Создается объект для сериализации, с помощью которого данные сериализуются и записываются в файл.
3. Реализован статический метод FindPass, который выполняет поиск пассажира по заданному условия (единица багажа одна и ее вес меньше 10). Файл открывается для чтения, затем данные десериализируются, т.е преобразование xml-файла в обратно в объектную структуру.
4. С помощью цикла перебираются все пассажиры, с заданным условием, если оно выполяется метод возвращает true, иначе false.
5. Данные тестируются и выводятся на экран.
### Тестирование
<img width="841" height="288" alt="image" src="https://github.com/user-attachments/assets/1faf0132-1fbc-4aa2-be2d-9d13f0e0372b" />

## Задание 6. List 
### Текст задачи
Даны упорядоченные списки L1 и L2. Вставить элементы списка L2 в список L1, не нарушая его
упорядоченности.
### Алгорит решения
1. Реализован статический метод ввода списка ReadList, который заполняется значениями, вводимыми пользователем с клавиатуры. Пользователь вводит количество элементов списка, после чего вводятся сами числа. После ввода список сортируется методом Sort, что обеспечивает упорядоченность.
2. Реализован метод вставки элементов InsertSorted, который принимает два упорядоченных списка. Для каждого элемента списка L2 выполняется поиск позиции вставки в списке L1, чтобы сохранить его упорядоченность. Для этого используется перебор элементов списка L1 и сравнение значений. После нахождения нужной позиции элемент вставляется с помощью метода Insert. Иначе элемент добавляется в конец списка.
3. Реализован метод вывода PrintList, который построчно выводит элементы списка с использованием цикла for.
### Тестирование
<img width="839" height="455" alt="image" src="https://github.com/user-attachments/assets/f7c09e64-9d6e-43d8-98b8-038ada15a554" />

## Задание 7. LinkedList 
### Текст задачи
Подсчитать количество элементов списка L, у которых равные «соседи».
### Алгорит решения
1. Реализован статический метод ввода списка ReadLinkedList, который заполняет список значениями, вводимыми пользователем с клавиатуры. Пользователь вводит количество элементов списка, после чего вводит элементы, которые добавляются в список с помощью метода AddLast.
2. Реализован метод обработки списка CountEqualNeighbors, который подсчитывает количество элементов, у которых равные «соседи». Для этого выполняется обход связного списка с использованием указателя на текущий узел. Проверка начинается со второго элемента и заканчивается предпоследним, так как только у них есть два соседа. Для каждого элемента сравниваются значение предыдущего и следующего узла. Если они равны, счётчик увеличивается.
3. Реализован метод вывода PrintLinkedList, который построчно выводит элементы связного списка.
### Тестирование
<img width="666" height="308" alt="image" src="https://github.com/user-attachments/assets/16442e3a-a170-4b61-82fa-3f6efd920250" />

## Задание 8. HashSet
### Текст задачи
Задан некоторый набор блюд в кафе. Определить для каждого из блюд, какие из них заказывали
все посетители, какие — некоторые из посетителей, и какие не заказывал никто.
### Алгорит решения
1. Метод ReadVisit считывает количество посетителей. Для каждого посетителя вводится количество блюд, затем названия блюд. 
2. Метод Dishes анализирует заказы посетителей и делит блюда на три группы:
some — все блюда, которые заказал хотя бы один посетитель (объединение)
all — блюда, которые заказали все посетители (пересечение)
none — блюда, которые не заказал никто (разность с исходным набором)
3. Для формирования some выполняется добавление всех блюд всех посетителей в одно множество.
4. Для формирования all изначально берётся полный набор блюд, затем из него удаляются те, которые отсутствуют у хотя бы одного посетителя.
5. Для формирования none из полного набора удаляются все блюда, которые встречаются хотя бы у одного посетителя.
6. Метод PrintSet выводит элементы множества на экран.
### Тестирование
<img width="723" height="467" alt="image" src="https://github.com/user-attachments/assets/1f90d788-0f52-4a47-9fd3-e0ad1103914b" />

## Задание 9. HashSet
### Текст задачи
Файл содержит текст на русском языке. Напечатать в алфавитном порядке все согласные буквы, которые входят ровно в одно слово.
### Алгоритм решения 
1. Реализован статический метод PrintConsonants, который считывает содержимое текстового файла. Затем текст разбивается на отдельные слова с помощью метода SplitWords.
2. После этого создаётся множество HashSet, которое используется для хранения итогового набора согласных букв с учётом их появления в словах.
3. Для каждого слова создаётся множество lettersInWord, в которое добавляются все уникальные согласные буквы, встречающиеся в данном слове.
4. Внутри обхода символов каждого слова выполняется проверка с помощью метода IsConsonant, который определяет, является ли символ русской согласной буквой. Если да — буква добавляется в lettersInWord.
5. После обработки слова выполняется обновление итогового множества:
   * если буква уже содержится,то  она удаляется;
   * если буква встречается впервые, она добавляется.
6. После завершения обработки всех слов выполняется вывод содержимого множества — на экран выводятся все согласные буквы, которые соответствуют итоговому условию программы.

### Тестирование
<img width="708" height="71" alt="image" src="https://github.com/user-attachments/assets/cfc21813-389b-4dfb-aeef-2a65a50d8b3d" />

## Задание 10. Dictionary/SortedList
### Текст задачи
На вход программы подаются сведения о результатах соревнований по школьному многоборью.
Многоборье состоит из соревнований по четырем видам спорта, участие в каждом из которых
оценивается баллами от 0 до 10 (0 баллов получает ученик, не принимавший участия в
соревнованиях по данному виду спорта). Победители определяются по наибольшей сумме
набранных баллов. Известно, что общее количество участников соревнований не превосходит
100.
В первой строке вводится количество учеников, принимавших участие в соревнованиях, N. Далее
следуют N строк, имеющих следующий формат:
<Фамилия><Имя><Баллы>
Здесь <Фамилия>– строка, состоящая не более чем из 20 символов;<Имя>– строка, состоящая не
более чем из 15 символов;<Баллы> - строка, содержащая четыре целых числа, разделенных
пробелом, соответствующих баллам, полученным на соревнованиях по каждому из четырех
видов спорта. При этом <Фамилия> и <Имя>, <Имя> и <Баллы> разделены одним пробелом.
Примеры входных строк:
Иванова Мария 5 8 6 3
Петров Сергей 9 9 5 7
Напишите программу, которая будет выводить на экран фамилии и имена трех лучших
участников многоборья. Если среди остальных участников есть ученики, набравшие то же
количество баллов, что и один из трех лучших, то их фамилии и имена также следует вывести.
При этом имена и фамилии можно выводить в произвольном порядке.
### Алгорит решения
1. Метод CompetitionTop3 считывает данные из текстового файла. В первой строке хранится количество участников n, которое преобразуется в число.
2. Далее выполняется обработка строк с данными участников. Каждая строка разбивается на части методом Split(' '). Первые два элемента объединяются в полное имя участника.
3. Остальные элементы строки содержат баллы. Они суммируются в переменную sum.
4. Для каждого участника выполняется накопление результата в словаре Dictionary<string, int>, где ключ — имя участника, а значение — сумма баллов. Если участник уже есть в словаре, его результат увеличивается.
5. После заполнения словаря выполняется поиск трёх лучших участников. В цикле три раза ищется максимальное значение среди всех элементов словаря.
На каждом шаге находится участник с наибольшим результатом - его имя и баллы выводятся на экран, после чего он удаляется из словаря, чтобы найти следующего победителя.
### Тестирование
<img width="749" height="286" alt="image" src="https://github.com/user-attachments/assets/cf16e8bc-05a1-4ed3-9b91-dba5e23b4c5d" />

